background yes

font 7x13
use_xft true
xftfont pango:{{ monofont }}:size={{ fontsize }}

total_run_times 0
own_window yes
own_window_type override
own_window_transparent yes
own_window_argb_visual true
double_buffer yes
use_spacer left
if_up_strictness address

update_interval 30

alignment bottom_left

TEXT
${color darkred}TODO:

${color darkgray}${exec todo -G | sed 's/  / / ; s/^ //'}
